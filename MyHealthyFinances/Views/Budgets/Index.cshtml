@model IEnumerable<MyHealthyFinances.Models.Budget>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BudDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BudValue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BudDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Group.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BudDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BudValue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BudDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Group.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BudgetId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BudgetId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BudgetId })
        </td>
    </tr>
}

</table>
